import 'package:flutter/material.dart';

class ContentData {
  String id;
  IconData icon;
  String topic;
  String image;
  String description;

  ContentData({this.id, this.icon, this.topic, this.image, this.description});
}
